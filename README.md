# Accessible React Select Component

Features:
- Fully accessible with keyboard
- No backdrop in `open` state
- `FormData` compliant

```
pnpm install     // install deps
pnpm dev         // run in dev mode
pnpm storybook   // start storybook
```
